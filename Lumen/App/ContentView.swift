import PDFVeCore
import SwiftUI

// 临时占位，将在 P-02 T-02-04 替换为 MainWindowView
struct ContentView: View {
    var body: some View {
        Text("Lumen")
            .frame(minWidth: 800, minHeight: 600)
    }
}
